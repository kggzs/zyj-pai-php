-- ============================================
-- 添加用户邀请链接配额字段
-- ============================================
-- 说明：用于存储用户通过积分兑换获得的额外邀请链接数量配额
-- ============================================

ALTER TABLE `users` ADD COLUMN `invite_quota_bonus` INT(11) DEFAULT 0 COMMENT '额外邀请链接配额（通过积分兑换获得）' AFTER `vip_expire_time`;

